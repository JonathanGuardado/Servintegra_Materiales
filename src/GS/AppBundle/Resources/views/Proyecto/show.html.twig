{% extends '::frontend.html.twig' %}

{% block title %} Principal Servintegra {% endblock %}

{% block id 'portada' %}

{% block main %}

<div class="container">

    <h1>Categoria {{ proyecto.nombre }}</h1>

    <dl class="dl-horizontal">
        <dt>Nombre</dt>
        <dd>{{ proyecto.nombre }}</dd>

        <dt>Descripcion</dt>
        <dd>{{ proyecto.personas }}</dd>

        <hr>

        <dt>&nbsp;</dt>
        <dd>
            <a href="{{ path('proyecto_edit', {'id': proyecto.id}) }}" class="btn btn-warning">Editar</a>
            <a href="{{ path('proyecto_delete', {'id': proyecto.id}) }}" class="btn btn-danger">Eliminar</a>
        </dd>

    </dl>

</div> <!-- /container -->

{% endblock %}