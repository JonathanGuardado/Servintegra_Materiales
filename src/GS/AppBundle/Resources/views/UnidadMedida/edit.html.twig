{% extends '::frontend.html.twig' %}

{% block title %} Principal Servintegra {% endblock %}

{% block id 'portada' %}

    {% block main %}
    <h1>Edici&oacute;n Unidad de medida</h1>

    {{ form(edit_form, {'attr': {'class': ' formulario'}}) }}

        <ul class="record_actions not_style_list">
        <li>
        <a  href="{{ path('unidadmedida') }}" class="btn btn-primary">
            Regresar al listado
        </a>
        </li>   
        </ul>
{% endblock %}
